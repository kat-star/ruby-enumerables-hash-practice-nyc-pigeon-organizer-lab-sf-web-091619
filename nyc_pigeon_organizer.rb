def nyc_pigeon_organizer(data)
  #create a hash with the bird names as keys (map over values to get names in array)
  #iterate over data hash (using each to do nested loops to iterate thru the nested hash data)
  #once I get to the array of names, push that attribute key into the new array as its value
  
  hash_values = data.values[0].value
  
  
end
